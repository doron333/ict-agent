worker: python -u main.py
# Railway runs ONE process type per service. To run NQ as its own service, create a second
# service on this repo with start command `python -u main.py --instrument nq` (see README).
nq: python -u main.py --instrument nq
